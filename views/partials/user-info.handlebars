<div class="vstack gap-3 align-items-stretch justify-content-center">
    <h1 class="text-center my-4">{{username}}</h1>
    <div class="text-center my-3">
        <img src="/images/upPlay.svg" width="188">
    </div>
    <div class="d-flex justify-content-center">
        <div class="row w-50 text-center">
            <h2 class="text-center col">Anthem{{anthem}}</h2>
            <h2 class="text-center col">Followers({{follower_count}})</h2>
        </div>
    </div>
    <h2 class="text-center my-4">Favorites({{favorite_count}})</h2>
    <h2 class="text-center my-4">Posts</h2>
    <h2 class="text-center my-4">Likes</h2>
    {{!-- {{#if (check_equal id, current_user.id)}}
    <button id="logout" type="button" class="text-end btn btn-danger">Log Out</button>
    {{/if}} --}}
</div>